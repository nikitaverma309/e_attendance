// enum Status { employeeNotFound, locationMismatch, success }

enum LoginStatus {
  employeeNotFound,
  locationMismatch,
  success,
  reRegisteredFace,
}

enum RegisterStatus {
  employeeNotFound,
  locationMismatch,
  success,
  alreadyRegistered,
  reRegisteredFace,
}